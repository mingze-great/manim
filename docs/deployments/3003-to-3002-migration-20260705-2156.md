# 3003 最新运行态迁移到 3002 记录

- 时间：2026-07-05 21:56 CST
- 操作目标：将当前 3003 已验证运行态迁移到 3002 生产服务
- 生产域名：www.lazymedia.cn
- 生产端口：3002
- 操作原则：只同步 3003 当前运行代码和前端产物，保留 3002 生产 `.env`、数据库、队列和运行数据

## 来源

### 3003 后端来源

- 路径：`/opt/manim-v2-3003-snapshot/backend`
- systemd：`manim-v2-3003-backend.service`
- WorkingDirectory：`/opt/manim-v2-3003-snapshot/backend`
- 端口：8003

### 3003 前端来源

- nginx root：`/opt/manim-v2-3003-repro-3002-current/frontend/dist`
- 前端源码：`/opt/manim-v2-3003-snapshot/frontend`

### 3003 Git 状态

- 分支：`feature/ai-video-module-on-3003`
- HEAD：`3310f23a3e22864f77ac67a630878b0126f2755f`
- 提交信息：`fix: resolve ai video storage root`
- 注意：3003 存在未提交运行态改动，本次迁移以服务器 3003 当前文件为准，不仅以 HEAD commit 为准。

## 迁移前 3002 备份

- 备份目录：`/opt/manim_backups/deploy_3003_to_3002_20260705_215628`
- 备份内容：
  - `/opt/manim-v2/backend`
  - `/opt/manim-v2/frontend`
  - `/opt/manim-v2/frontend/dist`
  - `/opt/manim-v2/backend/.env`
  - `/etc/nginx/sites-enabled/manim-v2.conf`
  - `/etc/nginx/sites-enabled/lazymedia-3002.conf`
  - 迁移前 git head/status
  - 来源 3003 git head/status

## 同步范围

### 已同步

- 3003 后端代码同步到：`/opt/manim-v2/backend`
- 3003 前端源码同步到：`/opt/manim-v2/frontend`
- 3003 当前运行前端 dist 同步到：`/opt/manim-v2/frontend/dist`

### 刻意保留/排除

- 保留 3002：`/opt/manim-v2/backend/.env`
- 保留共享数据库：`sqlite:////opt/manim/backend/manim.db`
- 保留 3002 队列：`CELERY_QUEUE=manim_v2`
- 保留 3002 后端端口：`PORT=8002`
- 排除运行数据和缓存：`storage`、`videos`、`app/videos`、`__pycache__`、`*.pyc`、`node_modules`

## 重启服务

```bash
systemctl stop manim-v2-worker.service manim-v2-backend.service
systemctl daemon-reload
systemctl start manim-v2-backend.service
systemctl start manim-v2-worker.service
```

## 验证结果

- `http://127.0.0.1:3002/`：200
- `http://127.0.0.1:3002/login`：200
- `http://127.0.0.1:3002/creator`：200
- `https://www.lazymedia.cn/`：200
- `https://www.lazymedia.cn/login`：200
- `http://127.0.0.1:3002/api/templates/public-preview?limit=200`：200
- 首页公开模板预览数量：57
- 带预览视频模板数量：57
- 公开接口未返回敏感字段：`code`、`prompt`、`reference_code`、`user_id`
- 抽查预览视频：
  - template_89：200 video/mp4
  - template_88：200 video/mp4
  - template_87：200 video/mp4
  - template_77：200 video/mp4
  - template_72：200 video/mp4
  - template_67：200 video/mp4
  - template_26：200 video/mp4
  - template_53：200 video/mp4
  - template_57：200 video/mp4
- 服务状态：
  - nginx：active
  - manim-v2-backend.service：active
  - manim-v2-worker.service：active
  - manim-v2-3003-backend.service：active
  - manim-v2-3003-worker.service：active

## 回退方式

如需回退本次迁移，优先使用备份目录：

```bash
backup_dir=/opt/manim_backups/deploy_3003_to_3002_20260705_215628
systemctl stop manim-v2-worker.service manim-v2-backend.service
rsync -a --delete $backup_dir/backend/ /opt/manim-v2/backend/
cp $backup_dir/backend.env /opt/manim-v2/backend/.env
rsync -a --delete $backup_dir/frontend/ /opt/manim-v2/frontend/
rsync -a --delete $backup_dir/frontend-dist/ /opt/manim-v2/frontend/dist/
systemctl daemon-reload
systemctl start manim-v2-backend.service manim-v2-worker.service
```

回退后重新验证首页、登录、工作台和模板预览接口。

## Git 固化状态

- 3002 本地快照提交：`ce62ccd766d4647c14dcf3ebdf255101e281a7b7`
- 提交信息：`deploy: migrate latest 3003 state to 3002`
- 当前分支：`repro/3002-current-20260705`
- 远程推送状态：服务器连接 GitHub 443 超时，当前本地分支比 `origin/repro/3002-current-20260705` ahead 1。
- 后续网络恢复后执行：

```bash
cd /opt/manim-v2
git push -u origin repro/3002-current-20260705
```
